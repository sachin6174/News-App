// `scripts/configure-local-env.sh` generates this ignored file from `.env`.
// This example only documents the resulting shape for somebody reading Git.
// Never put a real key in this tracked example file.
NEWS_API_KEY = paste_your_newly_rotated_key_here
