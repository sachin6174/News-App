// Debug builds may include a developer's ignored local settings file.
//
// The question mark is important: it means a fresh clone still builds even
// before the developer creates `Secrets.xcconfig`. The app then displays its
// friendly "missing token" error instead of Xcode failing mysteriously.
NEWS_API_KEY =
#include? "Secrets.xcconfig"
